# Прочитане на 3 променливи от тип цяло число, записване във списък и
# сумиране. На конзолата трябва да се принтира сумата.

first_number = input("Add first number:")
second_number = input("Add second number:")
number_number = input("Add third number:")

all_numbers = [int(first_number), int(second_number), int(number_number)]
print(sum(all_numbers)) 